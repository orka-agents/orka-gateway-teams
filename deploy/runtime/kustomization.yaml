# Use the parent deploy/ entrypoint; it generates the static proxy ConfigMap.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - configmap.yaml
  - deployment.yaml
  - services.yaml
  - networkpolicy.yaml
